services:
  voice-attributes:
    environment:
      MODEL_BACKEND: wavlm_onnx
      WAVLM_MODEL_PATH: /opt/models/wavlm/model.onnx
      WAVLM_MODEL_REVISION: ${WAVLM_MODEL_REVISION:-local-owned}
    volumes:
      - ./models/wavlm:/opt/models/wavlm:ro
